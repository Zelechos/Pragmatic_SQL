# To select all columns in table
SELECT *
FROM hello_sql.users;

# To select one column in table
SELECT name
FROM hello_sql.users;

# To select multiple columns in table
SELECT user_id, name
FROM hello_sql.users;

# Example I
SELECT email, password
FROM hello_sql.users;

# Example II
SELECT name, surname, age, init_date
FROM hello_sql.users;